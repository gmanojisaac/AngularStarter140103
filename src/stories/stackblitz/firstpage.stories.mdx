import { moduleMetadata } from '@storybook/angular';
import { Story, Meta, ArgsTable } from '@storybook/addon-docs';
import {  StackblitzTitleComponent  } from '../../app/youtube/2022sept02/stackblitz.component';
import { linkTo } from '@storybook/addon-links';
import { MarkdownModule, MarkdownService } from 'ngx-markdown';
import { HttpClient, HttpClientModule } from '@angular/common/http';
import { AppSharedModule } from '../../app/app-shared/app-shared.module';

# First Page shows the title of the youtube videos

Youtube video title is shown here and introduction about the video is spoken here

# First Page Mermaid diagram and small footer is shown

<Meta title="YoutubeMaking/06Sep22"   
  parameters={{
      viewMode: 'docs',
      previewTabs: { 
        canvas: { hidden: true } 
      },
  }}
  decorators={[
moduleMetadata({
  declarations: [StackblitzTitleComponent],
      imports: [
        AppSharedModule,
        HttpClientModule,
        MarkdownModule.forRoot({ loader: HttpClient }),
      ],
      providers: [MarkdownService],
    }),
    
    ]} />


<Story name="FirstPage">
  {{
    template: `<app-stackblitz-title [myselectedtemp]= "myselectedtemp"></app-stackblitz-title>`,
    props: {
    myselectedtemp : 'mermaidCenter'
    },
  }}
</Story>