import { moduleMetadata } from '@storybook/angular';
import { Story, Meta, ArgsTable } from '@storybook/addon-docs';
import { AboutMeComponent} from '../../app/about-me/about-me.component';
import { linkTo } from '@storybook/addon-links';
import { MarkdownModule, MarkdownService } from 'ngx-markdown';
import { HttpClient, HttpClientModule } from '@angular/common/http';
import { AppSharedModule } from '../../app/app-shared/app-shared.module';

# Started my career as a Lecturer in an Engg College

<Meta title="AboutMe/Lecturer"   
  parameters={{
      viewMode: 'docs',
      previewTabs: { 
        canvas: { hidden: true } 
      },
  }}
  decorators={[
moduleMetadata({
  declarations: [AboutMeComponent],
      imports: [
        AppSharedModule,
        HttpClientModule,
        MarkdownModule.forRoot({ loader: HttpClient }),
      ],
      providers: [MarkdownService],
    }),
    
    ]} />


<Story name="Lecturer">
  {{
    template: `<app-about-me [myselectedtemp]= "myselectedtemp"></app-about-me>`,
    props: {
    myselectedtemp : 'Lecturer'
    },
  }}
</Story>